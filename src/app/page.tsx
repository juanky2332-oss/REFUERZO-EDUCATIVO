import { Chat } from '@/components/Chat';

/**
 * La aplicación es la conversación.
 *
 * No hay portada que haya que atravesar ni menú donde elegir el camino: se
 * entra escribiendo o mandando la foto, que es lo que el alumno viene a hacer.
 */
export default function Inicio() {
  return <Chat />;
}
